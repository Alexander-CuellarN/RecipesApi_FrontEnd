export interface isAFavorite {
    idUsuario: number,
    idUser: number
}

export interface responseIsFavorite {
    message: string,
    data: boolean
}

export interface favoriteSctuture{
    idUsuario: number,
    idReceta: number 
}